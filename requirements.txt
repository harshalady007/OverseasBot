# Serverless/API dependencies (this file is what Vercel installs).
# For local development (Streamlit app, Excel loading, tests) use:
#   pip install -r requirements-local.txt
fastapi>=0.110
requests>=2.31
